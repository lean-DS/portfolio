steps:
- name: gcr.io/cloud-builders/docker
  args: ['build', '-t', '${_IMAGE}', '.']

images:
- '${_IMAGE}'

substitutions:
  _IMAGE: 'europe-west1-docker.pkg.dev/investor-recommender/portfolio1/portfolio-app:latest'

options:
  logging: CLOUD_LOGGING_ONLY
